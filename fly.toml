app = "leilabot"
primary_region = "syd"

[build]
  # Dockerfile in the repo will be used automatically

[env]
  # no non-secret env vars here

[processes]
  app = "python bot.py"

[vm]
  size = "shared-cpu-1x"
  memory = "256mb"
